# DECISIONES TÉCNICAS — GestorAcademia

> Registro **append-only** de decisiones técnicas tomadas de forma autónoma (antiguo §2 de
> SEGUIMIENTO). Es la sección que **sustituye a la revisión de código**: el dueño audita aquí.
>
> Disciplina del programador:
> - **Consulta** este documento (en el área que vas a tocar) ANTES de cualquier decisión técnica
>   no trivial, para no contradecir decisiones previas.
> - **Añade** cada nueva decisión relevante al cerrar la sesión (no edites ni borres filas previas).
> - Si una decisión es **norma permanente** ("usamos X, no reintroducir Y"), promuévela además a
>   §0.2 de `HOJA_DE_RUTA.md`, que se lee en cada sesión.
>
> En este proyecto hay dos áreas donde la decisión debe quedar SIEMPRE registrada, por ser las que
> el auditor contrasta: (a) todo lo relativo al esquema, políticas RLS y RPC — incluida la matriz
> rol × tabla × operación de T-10; y (b) el alcance del cliente propio de PostgREST (qué
> subconjunto se implementa y qué obligaría a ampliarlo), para que no crezca sin control.
>
> Rotación: si crece mucho, archivar por trimestre en `roadmap/DECISIONES_TECNICAS/AAAA-Tn.md`
> con un índice al inicio.

| Fecha | Tarea | Decisión | Alternativas consideradas | Por qué |
|-------|-------|----------|---------------------------|---------|
| 2026-08-26 | T-00 | TypeScript fijado en la línea **5.9.x** (`^5.9.3`), no la 7.0.x que acaba de publicarse | TypeScript 7.0.2 (el compilador nativo reescrito) | `typescript-eslint` 8.68.0 declara como peer `typescript >=4.8.4 <6.1.0`: instalar 7.0.x rompe `npm install` en cuanto se añade ESLint (necesario para T-01, que exige lint type-aware). 5.9.3 es la última estable de la línea que sí soporta todo el ecosistema type-aware que va a hacer falta. Revisar en una sesión futura si el ecosistero (`typescript-eslint`) ya soporta la 7.x antes de plantear la subida. |
| 2026-08-26 | T-00 | El código fuente importa módulos hermanos con extensión `.ts` (p. ej. `import { x } from './y.ts'`), no `.js`. `tsconfig.json` activa `allowImportingTsExtensions` + `rewriteRelativeImportExtensions` | (a) Importar con `.js` desde el origen, que es lo habitual, pero entonces `node --test` no puede ejecutar `src/**/*.test.ts` directamente (Node no reescribe `.js`→`.ts` al resolver, comprobado empíricamente: falla con `ERR_MODULE_NOT_FOUND`); (b) usar un bundler o `ts-node`/`tsx`, vetado por el stack fijado (§0.2, sin bundler, sin dependencias de runtime) | Node 22.22 hace *stripping* nativo de tipos por defecto (sin flag), así que puede ejecutar `.ts` directamente si los especificadores de import usan la extensión real del fichero en disco (`.ts`). `tsc` reescribe esas extensiones a `.js` en `dist/` al compilar, así que el navegador sigue cargando ES modules nativos válidos. Esto es lo que hace viable el requisito de T-03 de que `npm test` recorra `**/*.test.ts` sin paso de build ni `ts-node`. Documentado también en `DEVELOPERS.md`. |
| 2026-08-26 | T-00 | `tsconfig.json` único en la raíz, con `rootDir: "src"` — `herramientas/` queda **fuera** de su `include` por ahora (directorio vacío, solo `.gitkeep`) | Incluir ya `herramientas/**/*.ts` en el mismo `tsconfig.json` | `herramientas/` (el runner de migraciones) es código de Node puro, sin DOM, con su propio ciclo de vida (T-07); meterlo ya en el `include` habría forzado a decidir ahora mismo su libería/outDir sin tener código real que lo justifique. T-07 decide si lo añade a este mismo `tsconfig.json` (ampliando `include`) o si crea uno propio — queda anotado aquí para que esa sesión no lo pase por alto. |
| 2026-08-26 | T-00 | `eslint.config.js` mínimo (`@eslint/js` recomendado + `typescript-eslint` recomendado, sin reglas *type-aware* ni las reglas propias de guarda del stack) | No instalar ESLint todavía y dejar `npm run lint` fallando hasta T-01 | El protocolo exige la verificación pre-push completa (`typecheck && lint && test && build`) en cada push, incluido el de T-00. Sin un ESLint mínimo, `npm run lint` no existe como comando ejecutable. Esta configuración es deliberadamente mínima: T-01 la sustituye por la configuración estricta *type-aware* y añade las reglas que defienden el stack por herramienta (prohibir imports de terceros con `@supabase/supabase-js` nombrado explícitamente, prohibir `innerHTML`, prohibir `console.*` fuera del logger, prohibir `fetch` fuera de la capa de datos) y el hook de pre-commit — ninguno de esos requisitos de T-01 queda cubierto todavía. |
| 2026-08-26 | T-00 | Verificación de "la página abre en el navegador y ejecuta el módulo compilado" (criterio de aceptación de T-00) hecha con el Chromium headless preinstalado del entorno, lanzado por línea de comandos contra un servidor estático mínimo, sin añadirlo como dependencia del proyecto | Playwright/Puppeteer como `devDependency` | La lista de `devDependencies` de herramienta permitida (§0.2) es `typescript, eslint, @types/*, jsdom` — no incluye un driver de navegador. La comprobación fue manual, de esta sesión, no forma parte de la suite de tests (que en T-03 usará `jsdom`, sin red ni navegador real). |
| 2026-08-26 | T-01 | Las cuatro reglas que defienden el stack (paquetes de terceros en `src/`, `innerHTML`, `console.*` fuera del logger, `fetch` fuera de `src/datos/**`) se implementan con las reglas nativas de ESLint `no-restricted-imports`, `no-restricted-syntax` (selectores AST con regex de atributo) y `no-restricted-globals`, en `eslint.config.js` | Un plugin ESLint propio o de terceros (p. ej. `eslint-plugin-import`, `eslint-plugin-no-restricted-globals-por-carpeta`) | Ninguna regla nativa cubre exactamente "prohibir `fetch`/`console` salvo en una carpeta", pero componiendo reglas nativas + un bloque de override por `files: ['src/datos/**/*.ts']` (que apaga `no-restricted-globals` solo ahí) se consigue sin añadir ninguna dependencia — la política de `devDependencies` (§0.2) es la lista cerrada `typescript, eslint, @types/*, jsdom`, y un plugin nuevo no entra en ella. Verificado manualmente con ficheros de prueba (import de `@supabase/supabase-js`, `innerHTML`, `console.log`, `fetch`) que sí fallan el lint, y que el mismo `fetch` sí pasa dentro de `src/datos/`. |
| 2026-08-26 | T-01 | La configuración *type-aware* estricta (`strictTypeChecked` + `stylisticTypeChecked` de `typescript-eslint`, con `projectService`) se aplica solo a `files: ['src/**/*.ts']`; el resto del repo (`herramientas/**/*.ts`) recibe únicamente `tseslint.configs.recommended` sin chequeo de tipos | Extender ya `tsconfig.json` para incluir `herramientas/` y darle el mismo nivel de chequeo | `herramientas/` sigue fuera del `include` de `tsconfig.json` por la misma razón que en T-00 (T-07 decide su propio `tsconfig`/`outDir` cuando tenga código real, el runner de migraciones). `projectService` de `typescript-eslint` exige que cada fichero pertenezca a un proyecto TS; forzarlo sobre `herramientas/` antes de tiempo habría requerido inventar ese `tsconfig` ahora. El primer fichero real de `herramientas/` (`instalar-ganchos.ts`, el instalador del hook) queda así con parsing TS normal pero sin las reglas con tipos; T-07 decide si lo incorpora al chequeo estricto. |
| 2026-08-26 | T-01 | No se añade Prettier (ni ningún formateador) como `devDependency`; "formateo consistente" (requisito de T-01) se cubre con un `.editorconfig` en la raíz (sin dependencia alguna) más las reglas de estilo ya incluidas en `stylisticTypeChecked` de `typescript-eslint` | Prettier como `devDependency` nueva | La lista cerrada de `devDependencies` de herramienta permitida (§0.2) es `typescript, eslint, @types/*, jsdom`; Prettier no está en ella y añadirla sin decisión del dueño excede lo que el agente puede decidir por sí mismo en modo autonomía total para el *stack* fijado. `.editorconfig` cubre indentación, fin de línea LF, codificación UTF-8 y salto final sin herramienta nueva. |
| 2026-08-26 | T-01 | El hook de pre-commit se instala copiando un fichero versionado (`herramientas/git-hooks/pre-commit`) a `.git/hooks/pre-commit` mediante un script propio (`herramientas/instalar-ganchos.ts`) ejecutado por el hook `prepare` de npm en cada `npm install` | Husky (`devDependency` nueva, patrón estándar del ecosistema); o `git config core.hooksPath` para apuntar a un directorio versionado | Husky no está en la lista cerrada de `devDependencies` (§0.2). La alternativa de `git config core.hooksPath` habría requerido ejecutar `git config` sobre el repositorio, algo que el protocolo de esta sesión tiene vetado explícitamente; copiar el fichero a `.git/hooks/` con un script propio consigue el mismo resultado (`npm install` deja el hook activo) sin tocar la configuración de git ni añadir dependencias. Probado en esta sesión: `node herramientas/instalar-ganchos.ts` instala el hook con permisos de ejecución, y ejecutarlo directamente corre `typecheck && lint && test && build` en verde. |
| 2026-08-26 | T-02 | La depuración del `contexto` del logger (`src/nucleo/registro.ts`) es una lista de bloqueo por **nombre de campo** (regex de patrones: `nombre`, `apellido`, `email`, `telefono`, `avatar`, `ruta`, `token`, `password`, `clave`, `key`, `service_role`, etc., aplicada recursivamente a objetos y arrays anidados) más una defensa por **forma del valor** (JWT de tres segmentos, o cadena opaca ≥32 caracteres) independiente del nombre de la clave | (a) Lista de permitidos (allowlist) que solo deje pasar campos explícitamente marcados como identificadores; (b) no depurar nada y confiar en que quien llama al logger nunca pase datos personales | La spec de T-02 exige que el logger "nunca" emita esos datos, así que la protección tiene que vivir en el propio logger, no en la disciplina de quien lo llama (mismo principio que RLS en vez de ocultar botones). Una allowlist sería más segura en abstracto, pero exigiría que cada módulo de dominio declarase de antemano qué campos de cada objeto son "identificadores", acoplando el logger a los tipos del dominio antes de que existan (T-03 en adelante); la lista de bloqueo cubre hoy los tres tipos de dato prohibidos explícitos de §0.2 (personales, ruta de avatar, secretos) sin ese acoplamiento, y se amplía con una línea si aparece un campo nuevo. La defensa por forma del valor es la red de seguridad para cuando un secreto viaja bajo una clave genérica (`valor`, `header`, `dato`). El mensaje (`mensaje: string`) no se depura: es una cadena fija escrita por quien programa, igual que el nombre de una función; si algún día se interpola un dato de usuario ahí, es un fallo de quien escribe la llamada al logger, no algo que el logger pueda detectar en una cadena arbitraria. |
| 2026-08-26 | T-02 | Se añade `@types/node` (`^22.20.1`, la última de la línea 22.x, coherente con el `engines.node` de `package.json`) como `devDependency`, necesaria para tipar `import { test } from 'node:test'` y `node:assert/strict` en los tests | No tipar los tests (perder el chequeo de tipos de `npm run typecheck` sobre `src/**/*.test.ts`); o mover los tests fuera del `include` de `tsconfig.json` | `@types/*` está en la lista cerrada de `devDependencies` permitida (§0.2), así que no es una decisión que exceda la autonomía del agente. Se descartó la 26.x (la última publicada) porque no corresponde a ninguna versión real de Node: el proyecto fija Node ≥22.22, así que se usa la última de esa línea. Efecto colateral y su arreglo: `@types/node` declara globales ambientales (`process`, `Buffer`, `require`, `__dirname`, `__filename`) para **todo** el programa TypeScript, incluido el código de navegador de `src/ui` y `src/dominio`, donde no existen en tiempo de ejecución; se probó `"types": []` en `tsconfig.json` para evitarlo, pero eso también rompe la resolución de tipos de los propios especificadores `node:test`/`node:assert` (son parte del mismo paquete de tipos), así que se revirtió. La guarda queda en `eslint.config.js` (`no-restricted-globals`), coherente con cómo ya se defiende `fetch`: por herramienta, no por documento. |
| 2026-08-26 | T-03 | `npm test` ya recorría `src/**/*.test.ts` desde T-00; T-03 añade contenido real en los tres niveles exigidos (dominio, datos, UI) más el reloj inyectable, sin tocar la configuración del runner | — | El andamiaje de `node:test` ya existía; lo que faltaba era la disciplina de los tres niveles y el reloj. Se documenta aquí para que quede explícito que T-03 no es "crear el test runner" sino "poblarlo con cobertura real", que es justo lo que su criterio de aceptación exige. |
| 2026-08-26 | T-03 | Reloj inyectable: `Reloj` (interfaz con `ahora(): Date`) en `src/nucleo/reloj.ts`, con `relojDelSistema` (única implementación real, usa `new Date()`) y `crearRelojFijo` para tests. Guarda automática: `src/dominio/disciplinaReloj.test.ts` recorre por filesystem todo `.ts` de `src/dominio/` (excluidos los `.test.ts`) y falla si aparece `new Date()` sin argumentos o `Date.now()` | (a) Solo documentarlo en comentarios y confiar en la disciplina de quien programa; (b) una regla de ESLint que prohíba `new Date`/`Date.now` en `src/dominio/**` | La spec de T-03 exige "un test que falla si alguna función de dominio lee la hora del sistema en lugar del reloj inyectado" — una regla de ESLint cumpliría el espíritu pero no la letra ("un test"), y además una regla de ESLint no distingue `new Date()` (lectura del sistema, prohibida) de `new Date('2026-01-01')` (construir una fecha conocida, legítimo incluso dentro del dominio) tan bien como una comprobación explícita por regex que sí hace esa distinción. El test por filesystem es simple, no depende de ningún framework de análisis estático nuevo, y cubre exactamente lo que pide la spec. |
| 2026-08-26 | T-03 | Lógica de dominio mínima pero real, con el reloj inyectado: `src/dominio/slots.ts` (`slotVigente`, `slotActivoEnInstante`, `slotsQueTocanAhora`) y `src/dominio/asistencia.ts` (`esRetroactivo`, `puedeEditarAsistencia`). **Son una versión provisional e ilustrativa**: usan tipos locales propios (`SlotHorario`, `RegistroAsistencia`, `UsuarioAutenticado`), no los tipos oficiales del dominio (`src/dominio/tipos.ts`, que nace en T-07 punto 22 confrontado contra la forma real de PostgREST) | Esperar a T-07/T-15/T-17/T-18/T-21 para escribir cualquier lógica de dominio, dejando T-03 solo como configuración del test runner | La spec de T-03 pide explícitamente estos cuatro comportamientos como "prioritario desde el día 1": resolución de quién toca ahora, vigencia de slots, no-retroactividad del histórico, y reglas de quién edita qué registro — y el criterio de aceptación exige que los tres niveles tengan "al menos un test real", no un placeholder. T-15 (slots), T-17 (motor de propuesta) y T-18/T-21 (RPC de asistencia) reutilizarán y ampliarán estas funciones cuando existan los tipos oficiales y el esquema real; hasta entonces quedan como la base testeada que evita reinventar la regla de negocio en cada tarea posterior. |
| 2026-08-26 | T-03 | `slots.ts` calcula día de la semana y hora usando los métodos **UTC** de `Date` (`getUTCDay`, `getUTCHours`, `getUTCMinutes`), no la zona horaria local del proceso | Usar la zona horaria local del servidor (`getDay`/`getHours`); leer `ZONA_HORARIA_CENTRO` de `.env.ejemplo` | La zona horaria real del centro (`Europe/Madrid`, con cambio de horario de verano) y la ventana de tolerancia son pregunta abierta reservada para T-17 (ya anotada en el preámbulo de §6 de SEGUIMIENTO.md). Mientras tanto, usar la zona horaria local del proceso haría que los tests dependieran de la configuración del entorno que los ejecuta — justo lo que el corolario de esta sesión prohíbe ("todos los tests deben funcionar sin ninguna credencial ni variable de entorno"). UTC es determinista en cualquier máquina. Cuando T-17 resuelva la zona horaria, `slotActivoEnInstante` pasa a recibir la zona horaria como parámetro explícito en vez de asumir UTC. |
| 2026-08-26 | T-03 | Doble del cliente HTTP para el nivel "datos": `crearFetchSimulado(manejador)` en `src/datos/pruebas/dobleHttp.ts`, compatible con la firma de `fetch` (`(url, init) => Promise<Response>`), construido sobre el `Response` global de Node (sin dependencia nueva); más `crearFetchSimuladoConErrorDeRed` para simular un fallo de red (promesa rechazada, no una respuesta) | (a) Una librería de mocking HTTP como `nock` o `msw` (`devDependency` nueva); (b) doblar cada función de la futura capa de datos por separado, sin una pieza compartida | Ninguna librería de mocking está en la lista cerrada de `devDependencies` (§0.2: `typescript, eslint, @types/*, jsdom`). Como el cliente propio de T-08 va a recibir la implementación de red por inyección (para poder testearse sin tocar Supabase), un doble que imite la firma exacta de `fetch` es lo mínimo que hace falta y es reutilizable tal cual por T-08 sin tener que rehacerlo. Vive en `src/datos/pruebas/` (dentro de `src/datos/**`, donde `fetch` está permitido) para no necesitar una excepción de ESLint adicional, y no es un fichero `.test.ts`: es infraestructura de test importada desde los tests, no un test en sí. |
| 2026-08-26 | T-03 | Nivel "UI": `src/ui/main.ts` se reduce a leer `#app` y delegar en `mostrarPantallaInicial(contenedor)` (`src/ui/pantallaInicial.ts`), una función pura sobre un `HTMLElement` ya obtenido. El test monta el contenedor con `jsdom` (`new JSDOM(...)`) y afirma sobre nodos reales, sin tocar el `document` global de Node | Testear `main.ts` tal cual, sustituyendo el `document` global de Node por el de `jsdom` antes de importar el módulo (con efectos de importación en el ámbito superior) | La spec de T-03 pide "helpers propios sobre jsdom... para montar un contenedor y afirmar sobre nodos reales". Un módulo con lógica en el ámbito superior (como estaba `main.ts` hasta ahora) no se puede importar dos veces con estados de DOM distintos ni testear sus ramas por separado; extraer la función a un módulo aparte que recibe el contenedor como parámetro es el patrón mínimo que lo hace testeable sin añadir un framework de UI (el stack sigue siendo DOM nativo, T-03 no cambia eso). `main.ts` queda como el único punto que toca el `document` global real. |
| 2026-08-26 | T-03 | `jsdom` (y `@types/jsdom`) añadidas como `devDependency`; el import de `jsdom` está vetado por la regla general "sin paquetes de terceros en `src/`" (T-01), así que se añade un override de ESLint específico para `files: ['src/**/*.test.ts']` que reescribe `no-restricted-syntax` para permitir únicamente el especificador `jsdom`, manteniendo intactas las demás restricciones (otros paquetes de terceros, `innerHTML`, `console.*` fuera del logger) | Desactivar `no-restricted-imports`/`no-restricted-syntax` por completo para todo `*.test.ts` | La lista cerrada de `devDependencies` de test (§0.2) es exactamente `jsdom` para tests de DOM: la excepción de ESLint debe ser tan estrecha como esa lista, no una vía abierta para que un test futuro importe cualquier otra dependencia sin que el lint lo detecte. El override reconstruye el mismo conjunto de selectores con el nombre `jsdom` añadido a la negación del regex, en vez de apagar la regla. |
| 2026-08-26 | T-04 | Versión de Node fijada en un `.nvmrc` nuevo en la raíz (`22.22.2`, la misma instalada en esta sesión), leído tanto por el workflow de CI (`actions/setup-node` con `node-version-file: '.nvmrc'`) como por cualquier desarrollador con `nvm` | Escribir la versión directamente en el YAML del workflow (`node-version: '22.22.2'`); dejar que CI use `engines.node` de `package.json` (`>=22.22.0`, un mínimo, no una versión fija) | La spec de T-04 exige "fijando la versión de Node". `engines.node` es intencionadamente un mínimo abierto (no lo cambia esta tarea); un fichero `.nvmrc` aparte dale una única fuente de verdad para la versión exacta usada en desarrollo y en CI sin acoplar ambos significados en el mismo campo, y sin añadir ninguna dependencia (es un fichero de texto plano, no software) |
| 2026-08-26 | T-04 | Workflow de GitHub Actions (`.github/workflows/ci.yml`) con un único job que corre `npm ci` y luego `typecheck`, `lint`, `test`, `build` en ese orden, disparado por `push` a `develop` y a `master`, sin declarar ningún `secrets`/`env` | Añadir también el trigger `pull_request` (no hay flujo de PR todavía, modo AUTONOMÍA TOTAL, §0.1); usar `npm install` en vez de `npm ci` | La spec de T-04 pide exactamente "en cada push a `develop` y a `master`"; añadir `pull_request` sería alcance no pedido en un repositorio que hoy no abre PRs contra `develop`. `npm ci` es determinista contra `package-lock.json` (ya commiteado desde T-00) y es la práctica estándar en CI. No se declara ningún secreto porque, verificado en esta sesión con `env -i`, la verificación completa (`typecheck && lint && test && build`) pasa sin ninguna variable de entorno — igual que exige el corolario de T-03 para la suite de tests |
| 2026-08-26 | T-04 | `actions/checkout@v7` y `actions/setup-node@v6` (versiones mayores vigentes en la fecha de esta sesión, comprobadas por búsqueda, no asumidas de memoria) | Fijar versiones anteriores (v4/v5) por ser las más citadas en documentación de entrenamiento | El entorno de esta sesión tiene fecha 2026-08-26; usar una versión de acción desactualizada de memoria arriesga pinnear un tag que Marketplace ya no recomienda. Se confirmó por búsqueda web que v7 (checkout) y v6 (setup-node) son las versiones mayores actuales antes de escribir el workflow |
| 2026-08-27 | T-05 | Arquitectura en tres piezas: `src/nucleo/informadorErrores.ts` (`crearInformadorErrores(logger, enviar?)`, puro, sin DOM ni red) que depura y registra en local; `src/nucleo/capturaErrores.ts` (`instalarCapturaErrores(objetivo, informador)`) que conecta los eventos globales `error`/`unhandledrejection` de un `objetivo` inyectado (nunca lee `window` directamente); y `src/datos/eventoError.ts` (`crearEnviadorEventoError(config)`) que implementa el envío real a la RPC por `fetch`, la única pieza con red y por tanto la única en `src/datos/**` | Un único módulo que mezclase captura de eventos, depuración y `fetch` | Mantiene la regla de ESLint de T-01 (`fetch` solo en `src/datos/**`) sin excepción nueva, y hace que `informadorErrores.ts`/`capturaErrores.ts` se testeen sin `jsdom` real de red ni doble de `fetch` — cada pieza se testea con el doble mínimo que le corresponde (logger de pruebas, `EventTarget` de `jsdom`, `crearFetchSimulado` de T-03 respectivamente). Sigue el mismo patrón de inyección que `Reloj` (T-03) y `SumideroLog` (T-02): nunca alcanzar un global directamente desde el código de dominio/núcleo. |
| 2026-08-27 | T-05 | El `contexto` que pasa a `evento_error`, y también `mensaje`/`pila` (a diferencia del `mensaje` del logger de T-02, que es una cadena fija escrita por quien programa, aquí `mensaje`/`pila` vienen de una excepción real y pueden contener cualquier cosa), se depuran juntos reusando `depurarContexto` de `src/nucleo/registro.ts` (T-02): se construye un objeto `{ origen, mensaje, pila, ...contexto }` y se pasa entero por `depurarContexto` antes de salir del informador | Escribir una función de depuración nueva y propia para `evento_error` | `depurarContexto` ya cubre exactamente lo que pide la spec de T-05 (nombre, teléfono, ruta de avatar, token) tanto por nombre de campo como por forma del valor (JWT/cadena opaca) — esta última defensa es la que protege `mensaje`/`pila` aunque no vayan bajo una clave sensible. Reutilizarla evita mantener dos listas de patrones sensibles sincronizadas a mano. |
| 2026-08-27 | T-05 | Contrato de la RPC `registrar_evento_error` que T-07 debe implementar en `001_esquema_inicial.sql` junto con la tabla `evento_error`: parámetros `p_origen` (`'no_controlado' \| 'promesa_rechazada' \| 'capa_datos'`), `p_mensaje` (texto), `p_pila` (texto, nullable), `p_contexto` (jsonb, nullable) — la RPC fija ella misma `registrado_en` y el autor vía `auth.uid()`, igual que `registrar_asistencia`; lectura reservada a `administrator` por RLS | Dejar el contrato sin fijar hasta T-07 | T-05 depende solo de T-02 (no de T-07) según el resumen de dependencias de `HOJA_DE_RUTA.md`, así que su cliente (`src/datos/eventoError.ts`) se escribe y se testea ya, contra un doble de `fetch` — pero necesita un contrato fijo para no tener que rehacerse cuando T-07 escriba la SQL real. Se fija aquí para que esa sesión futura solo tenga que hacerlo coincidir, no diseñarlo desde cero. |
| 2026-08-27 | T-05 | `src/ui/main.ts` instala la captura global (`instalarCapturaErrores(window, crearInformadorErrores(logger))`) **sin** `enviar`: hoy solo registra en el logger local (ya depurado), porque no existe todavía ni la tabla `evento_error` (T-07) ni un cliente Supabase real con URL/clave anónima que inyectar a `crearEnviadorEventoError` (T-08) | Esperar a T-08 para instalar la captura en `main.ts` | El valor de "capturar y loggear en local" es real e independiente de T-07/T-08 (un error no controlado hoy ya queda registrado, aunque no persista en servidor), y no compromete nada: cuando T-08 exista, la única integración pendiente es pasar el `enviar` real como segundo argumento en este mismo punto. Verificado en Chromium headless (mismo método que T-00) que la página sigue cargando sin error tras esta instalación. |
| 2026-08-27 | T-05 | Recorte de comportamiento del guardado por defensa contra recursión: `informar()` nunca deja una promesa de `enviar` sin `.catch()` (ni sin `try/catch` para un fallo síncrono), y ante un fallo de envío llama a `logger.warn` **una sola vez**, nunca vuelve a llamar a `informar()` — un test simula `enviar` rechazando y otro lanzando síncronamente, y ambos comprueban que no hay una segunda llamada ni un rechazo sin capturar (`process.on('unhandledRejection', ...)` temporal en el test) | Reintentar el envío una vez antes de rendirse | La spec de T-05 exige explícitamente que "la escritura de errores debe tolerar su propio fallo sin provocar un bucle" — un reintento automático added complejidad (¿cuántos intentos?, ¿con qué retroceso?) que además se solapa con T-06 (límites de abuso y robustez, que sí define el retroceso exponencial genérico de escritura). Se deja el reintento para cuando T-06 aporte esa pieza compartida, en vez de improvisar una versión propia aquí. |
| 2026-08-27 | T-05 | `@types/jsdom` no tipa `PromiseRejectionEvent` como propiedad de `DOMWindow` (a diferencia de `ErrorEvent`, que sí lo tiene: `ErrorEvent: typeof ErrorEvent`); cae en el índice `[key: string]: any` de esa interfaz. El test de `capturaErrores.test.ts` que dispara `unhandledrejection` hace `ventana.PromiseRejectionEvent as typeof PromiseRejectionEvent` (el tipo global de la librería `DOM`, no un valor — el valor real sigue siendo el de la instancia de `jsdom`) para no propagar `any` y mantener el lint estricto en verde | Desactivar `no-unsafe-*` para ese test; usar el `PromiseRejectionEvent` global de Node (no existe fuera de un navegador/`jsdom`, así que esa opción no es viable en absoluto) | Comprobado leyendo `node_modules/@types/jsdom/index.d.ts`: `DOMWindow` declara explícitamente `ErrorEvent` pero no `PromiseRejectionEvent`, así que este último resuelve por el índice genérico `any` de la interfaz. La aserción de tipo es la forma mínima de recuperar el tipo correcto sin apagar ninguna regla de ESLint. |
| 2026-08-27 | T-06 | Arquitectura en seis piezas nuevas de `src/nucleo/`, todas sin red ni DOM y por tanto testeables sin dobles de infraestructura: `limitadorTasa.ts` (contador por clave y ventana fija, con `Reloj` inyectado, lanza `ErrorLimiteAlcanzado` identificable), `proteccionDobleToque.ts` (deduplica llamadas concurrentes a una misma operación asíncrona), `temporizador.ts` (interfaz `Temporizador` nueva, hermana de `Reloj`), `reintento.ts` (retroceso exponencial acotado sobre `Temporizador`), `controlPeticion.ts` (cancelación de la petición obsoleta y tiempo de espera, sobre `AbortController` nativo) y `mensajesAbuso.ts` (traducción a español de los errores que introduce esta tarea) | Un único módulo "robustez.ts" que mezclase las cinco responsabilidades | Mismo criterio que T-05 (informadorErrores/capturaErrores/eventoError en piezas separadas): cada responsabilidad se testea con el doble mínimo que le corresponde (reloj fijo, temporizador de prueba, un `AbortController` real) sin arrastrar las demás, y cada pieza queda reutilizable por separado desde cualquier punto futuro (T-14, T-18, T-19, T-21) sin que un cambio en una obligue a tocar las otras. |
| 2026-08-27 | T-06 | Nueva interfaz `Temporizador` (`src/nucleo/temporizador.ts`) con contrato `esperar(ms): Promise<void>`, distinta de `Reloj` (T-03), para el retroceso exponencial | Reutilizar `Reloj` también para las esperas del backoff, ya que la spec de T-06 dice literalmente "test del retroceso exponencial con reloj inyectado" | `Reloj.ahora()` responde "qué hora es ahora", una lectura instantánea; el backoff necesita "espera este rato", una acción con duración. No hay forma de derivar una espera determinista y sin tiempo real de un `Reloj` que solo expone el instante actual — se probó mentalmente y no compone. `Temporizador` seguido el mismo patrón de inyección exacto (`temporizadorReal` con `setTimeout`, confinado a los puntos de entrada; `crearTemporizadorDePrueba` para tests, que no espera de verdad y deja registrado cuánto se le pidió esperar). El test de `reintento.test.ts` es igual de determinista y rápido que si usara un `Reloj` — cumple el espíritu de la frase de la spec (reintentos verificables sin esperar tiempo real en el test) aunque la pieza inyectada tenga un nombre distinto. |
| 2026-08-27 | T-06 | Contrato recomendado, para cuando T-14/T-18/T-21 escriban sus RPC reales, del límite de "operaciones por usuario y minuto" (requisito 2 de T-06): `registrar_asistencia` y `actualizar_asistencia` — 60 operaciones por profesor y minuto (1/segundo sostenido: generoso para marcar de un tirón una clase de 30 alumnos, bloquea igualmente un script automatizado); subida de avatar — 20 subidas por `administrator` y hora (las fichas se crean o se editan por lotes ocasionales, no en volumen). El error debe ser identificable igual que `ErrorLimiteAlcanzado` de `limitadorTasa.ts` (aquí, del lado de PostgreSQL, un código de error o un `RAISE EXCEPTION` con un `SQLSTATE`/mensaje reconocible, no un texto libre) | Dejar el número sin fijar hasta la sesión de T-18/T-21/T-14 | Mismo motivo que el contrato de `registrar_evento_error` fijado por T-05 para T-07: T-06 no escribe SQL (`Migración: No` en su spec) porque las RPC de escritura son T-18/T-21 y la subida de avatar es T-14, todas todavía sin migración: fijar aquí un número de partida razonable, justificado por el uso real (pasar lista de una clase), evita que esa sesión futura tenga que inventarlo desde cero o dejarlo sin límite por prisa. La cifra es un punto de partida, no una decisión cerrada: la sesión que implemente la RPC puede ajustarla si la experiencia real de aula lo pide, y debe anotarlo aquí si lo hace. |
| 2026-08-27 | T-06 | `mensajesAbuso.ts` (`mensajeAmigable(error)`) solo traduce los dos errores que esta tarea introduce (`ErrorLimiteAlcanzado`, `AbortError` de cancelación/tiempo de espera); cualquier otro error cae en un mensaje genérico | Esperar a T-08 para escribir cualquier traducción de error a mensaje de usuario, ya que ahí se define la taxonomía completa (`NoAutenticado`, `SinPermiso`, `Conflicto`, `ErrorDeValidacion`, `ErrorDeRed`, `ErrorDelServidor`, `FicheroDemasiadoGrande`, `TipoDeFicheroNoPermitido`) | El requisito 4 de T-06 ("mensajes al usuario en español que expliquen qué hacer, no el código de error") aplica ya a los errores que esta misma tarea produce, así que no tiene sentido dejarlos sin traducir mientras se espera a T-08. La función queda deliberadamente abierta a que T-08 añada sus propios casos (`if (error instanceof ...)` adicionales) en vez de inventar aquí una traducción para tipos de error que todavía no existen. |
| 2026-08-27 | T-06 | Investigación de los límites de intentos de Supabase Auth (requisito 1 de T-06): GoTrue usa un algoritmo de *token bucket* por endpoint, con los límites de envío de correo (`/auth/v1/signup`, `/auth/v1/recover`, `/auth/v1/user`) y de OTP/enlace mágico configurables desde el panel (**Authentication → Rate Limits**) o por la Management API, y los límites de `/auth/v1/verify`, `/auth/v1/token` (que es también el endpoint del inicio de sesión con contraseña, `grant_type=password`) y los desafíos de MFA limitados **por IP** y **no configurables desde el panel**. GoTrue **no tiene** un bloqueo de cuenta tras N contraseñas incorrectas: la única defensa por defecto contra fuerza bruta al iniciar sesión es ese límite por IP de `/auth/v1/token`, no un límite por email. No se ha podido confirmar la cifra numérica exacta vigente hoy (ver limitación abajo): anotado en §6 de SEGUIMIENTO.md para que el dueño lo revise en el panel de su proyecto antes de T-25 (paso a producción) y, si hace falta un límite por cuenta además del de IP, se trate como pregunta de producto para T-09 | Asumir de memoria una cifra numérica concreta y documentarla como si fuera un hecho verificado | El protocolo de esta sesión exige no inventar hechos verificables. Esta sesión no tiene salida de red hacia `supabase.com` (bloqueada por el proxy de egress del entorno) y el espejo en bruto de la documentación en GitHub (`raw.githubusercontent.com/supabase/supabase/.../rate-limits.mdx` y su parcial `auth_rate_limits.mdx`) solo se pudo leer a través de una herramienta que resume con un modelo pequeño, que no reprodujo los números exactos de la tabla (se negó a citarlos verbatim). Se documenta aquí lo que sí quedó confirmado por dos fuentes independientes (la página de documentación y su código fuente en el repositorio de Supabase) — el algoritmo, qué endpoints son configurables y cuáles no, y el hecho de que no existe bloqueo por cuenta — y se deja explícito que la cifra exacta no se pudo verificar, en vez de rellenarla con un número plausible. |
| 2026-08-26 | T-03 | `npm run build` pasa a usar `tsconfig.build.json` (que extiende `tsconfig.json` y excluye `src/**/*.test.ts`) en vez del `tsconfig.json` base; `npm run typecheck` sigue usando el `tsconfig.json` base sin excepción, así que los tests se siguen comprobando de tipos | Dejar `npm run build` con el `tsconfig.json` base, como estaba | Antes de esta tarea, `dist/nucleo/registro.test.js` ya se compilaba y se publicaba en el build estático (efecto colateral inofensivo porque no importaba nada externo). Con `jsdom` como nueva dependencia de test, `dist/ui/pantallaInicial.test.js` habría llevado un `import { JSDOM } from 'jsdom'` — un especificador de paquete que no existe como módulo servible en el navegador — al `dist/` que el push a `develop` despliega al hosting estático (§0.1). No rompe nada porque `index.html` solo carga `dist/ui/main.js`, pero es basura de test filtrándose al build de producción. Separar el `tsconfig` de build es el cambio mínimo: no toca `typecheck` (los tests se siguen comprobando de tipos) ni `lint`, solo qué emite `tsc` en modo build. |
| 2026-08-27 | T-07 | `herramientas/` recibe su propio `tsconfig.herramientas.json` (Node puro: `lib: ["ES2022"]`, `types: ["node"]`, sin DOM) y el mismo nivel de ESLint estricto *type-aware* que `src/` (`strictTypeChecked` + `stylisticTypeChecked`), con `parserOptions.project` explícito apuntando a ese fichero — decisión pendiente que T-01 dejó abierta para cuando hubiera código real de `herramientas/` que la justificara (el runner de migraciones) | (a) Dejar `herramientas/` sin chequeo de tipos, como hasta ahora; (b) usar `projectService: true` igual que en `src/**/*.ts` | (a) el runner es la pieza que aplica DDL contra la base de datos real: quererlo sin chequeo de tipos era la opción menos defendible de todo el proyecto. (b) se probó primero y falló: `projectService` busca el `tsconfig.json` más cercano **por nombre** subiendo directorios, así que para cualquier fichero de `herramientas/` encontraba el `tsconfig.json` de `src/` (que no lo incluye) en vez de `tsconfig.herramientas.json`, y ESLint fallaba con "was not found by the project service" en todos los ficheros. `project: './tsconfig.herramientas.json'` (explícito) sí funciona porque no depende de la búsqueda por nombre. `npm run typecheck` pasa a ser `tsc --noEmit && tsc --noEmit -p tsconfig.herramientas.json` (dos proyectos, uno por árbol). |
| 2026-08-27 | T-07 | `db/001_esquema_inicial.sql` es DDL **plano**: sin `begin`/`commit` propios ni alta en `esquema_migracion` al final, a diferencia de `000_bootstrap_perfil.sql` (autocontenido, aplicado a mano). El runner (`herramientas/migraciones/runner.ts`, función `construirSqlTransaccional`) es quien envuelve el contenido del fichero en `begin; <contenido>; insert into esquema_migracion (...); commit;` antes de enviarlo a la Management API, con el hash SHA-256 que él mismo calcula del fichero tal cual está en disco | Que cada migración incluya su propio `begin`/`commit` y su propio `insert into esquema_migracion`, igual que el bootstrap | El hash de inmutabilidad se calcula sobre el contenido del fichero; si el fichero incluyera su propia fila de ledger con el hash dentro, sería circular (el hash formaría parte de lo que hashea). Dejar que el runner añada la transacción y el alta en el ledger en la misma llamada a la Management API garantiza que un DDL que falla nunca deja una fila de ledger huérfana, y viceversa — la migración 001 no necesita saberlo. A partir de 001 esta es la convención: los ficheros que aplica el runner son DDL plano; solo 000/000b (bootstrap manual, fuera del runner) se autocontienen. |
| 2026-08-27 | T-07 | El endpoint de la Management API que usa `herramientas/migraciones/clienteManagementApi.ts` (`POST https://api.supabase.com/v1/projects/{ref}/database/query`, cuerpo `{ query: sql }`) **no se ha podido verificar contra documentación en vivo** en esta sesión — igual que T-06 con los límites de Auth, sin salida de red hacia `supabase.com`. Queda documentado en el propio fichero y aquí para que si el dueño ve un `404` al ejecutar `npm run migrate`, sepa que es el primer sospechoso, sin tener que leer el código para encontrarlo | Inventar un endpoint distinto sin decirlo, o bloquear T-07 entera hasta poder verificarlo | El resto del runner (guardas de contenido, hash de inmutabilidad, orquestación de pendientes, salvaguarda de `prod`) no depende de que el endpoint sea exacto: está aislado en una única función y testeado contra un doble de `fetch`, así que corregirlo si hace falta es un cambio de una línea. Bloquear toda la tarea por una incertidumbre acotada y ya señalada con transparencia habría sido peor que seguir con la limitación documentada. |
| 2026-08-27 | T-07 | Las guardas de contenido del runner (`herramientas/migraciones/guardas.ts`) corren sobre el SQL **sin comentarios** (`quitarComentariosSql`, que quita `-- ...` y `/* ... */` antes de aplicar los patrones), y la guarda "DROP POLICY sin CREATE POLICY" empareja por **nombre de política** (extrae el nombre de cada `drop policy` y de cada `create policy` y compara conjuntos), no solo por presencia de las palabras clave | Aplicar los patrones directamente sobre el fichero tal cual, sin quitar comentarios; emparejar DROP/CREATE POLICY solo por presencia global de ambas palabras en el fichero | Sin quitar comentarios, una frase explicativa como "sin UPDATE ni DELETE para nadie" (que aparece varias veces en `001_esquema_inicial.sql` documentando el propio invariante) dispararía la guarda sobre su propia documentación. Emparejar por nombre de política es más fiel al requisito literal de T-07 ("un DROP POLICY sin CREATE POLICY en el mismo fichero") que emparejar por presencia global, que dejaría pasar un fichero con un DROP de una política y un CREATE de otra distinta. |
| 2026-08-27 | T-07 | El punto 20b de T-07 pide un test que recorra `information_schema.role_table_grants` — imposible dentro de `npm test` porque exige una conexión real (el corolario de esta sesión: toda la suite corre sin ninguna credencial). Se resuelve en dos piezas: (a) `herramientas/migraciones/esquemaInicial.test.ts`, un test **estático** y hermético que parsea el propio `db/001_esquema_inicial.sql` y falla si algún `GRANT` da `TRUNCATE`/`REFERENCES`/`TRIGGER` a `anon`/`authenticated`, si falta un `enable row level security` o un `revoke all` explícito por tabla — cubre el requisito de aceptación de T-07 sin credenciales; (b) `herramientas/migraciones/verificarPrivilegios.ts`, un modo adicional del mismo runner (`npm run migrate -- --verificar-privilegios`) que sí hace la consulta en vivo contra `information_schema.role_table_grants`, pensado para que el dueño lo ejecute tras aplicar una migración — mismo régimen de credenciales que `npm run migrate` | Omitir el barrido en vivo hasta T-10 (que ya trae `npm run probar-rls`); o forzar el test estático a ser el único mecanismo, sin operativa en vivo | El barrido en vivo es la única comprobación que detecta de verdad si Supabase concedió algo por defecto que el SQL no pidió explícitamente (el fallo real que ya ocurrió una vez, documentado en `000b_arreglo_permisos.sql`); el test estático solo puede comprobar que el SQL no lo hace **a propósito**. Ninguna de las dos piezas sustituye a la otra. Se integra en el mismo runner de migraciones en vez de crear una herramienta nueva porque comparte exactamente las mismas credenciales y el mismo cliente de la Management API. |
| 2026-08-27 | T-07 | `src/dominio/tipos.ts` (tipos oficiales, escritos a mano) se confronta con la forma de PostgREST en `src/dominio/tipos.test.ts` mediante comparación de **conjuntos de claves** (`Object.keys(...).sort()`) sobre una fila de ejemplo simulada con el doble de `fetch` de T-03, tabla por tabla, no mediante un validador de esquema completo (tipo/nulabilidad por campo) | Escribir un validador de forma completo (comprobar tipo y nulabilidad de cada campo, no solo el nombre); o no testear la forma en absoluto y confiar en la revisión visual | El criterio de aceptación de T-07 pide confrontar la forma "que devuelve PostgREST según el doble", y PostgREST no transforma nombres de columna: el riesgo real que este test cubre es que alguien añada, quite o renombre una columna en el SQL sin actualizar `tipos.ts` (o viceversa). Un validador de tipo/nulabilidad completo sería más estricto pero exigiría mantener dos veces la misma información (el `CHECK`/`nullable` de la columna y el validador), y esta tarea no tiene todavía ningún punto de la aplicación que consuma estos tipos para poder detectar esa clase de error de otra forma — se deja para cuando T-08 (cliente PostgREST) o T-11/T-12 (primeras pantallas) puedan ejercitarlo de verdad. |
| 2026-08-27 | T-07 | **Las propiedades de parámetro de TypeScript (`constructor(readonly x: string)`) no funcionan en este proyecto**, en ningún fichero, ni siquiera fuera de `src/`: Node 22 ejecuta los `.test.ts` con *type-stripping* nativo (decisión de T-00), que no soporta esa sintaxis y falla en tiempo de ejecución con `ERR_UNSUPPORTED_TYPESCRIPT_SYNTAX: TypeScript parameter property is not supported in strip-only mode` — un error que **no aparece en `tsc --noEmit` ni en ESLint**, solo al ejecutar `npm test` de verdad. Las cuatro clases de error nuevas de esta sesión (`ErrorManagementApi`, `ErrorGuardaContenido`, `ErrorHashCambiado`, `ErrorClienteAdmin`) se reescribieron con el patrón ya usado por `ErrorLimiteAlcanzado` (T-06): campos declarados aparte y asignados a mano en el cuerpo del constructor | Mantener las propiedades de parámetro, ya que compilan y pasan el lint sin aviso | Se descubrió al ejecutar la suite completa (no al `typecheck` ni al `lint`, que no la detectan): las cuatro clases nuevas hacían fallar sus ficheros de test con un error de proceso, no un fallo de aserción. Como no hay ningún test previo en el repositorio que use este patrón, no había precedente hasta ahora — queda registrado aquí explícitamente (y no solo en el código) para que ninguna sesión futura lo reintroduzca en una clase de error nueva en cualquier carpeta del proyecto, no solo en `src/`. |
| 2026-08-27 | T-07 | La semilla de desarrollo (`npm run seed`, `herramientas/seed.ts`) necesita la clave `service_role` para crear usuarios reales (API admin de GoTrue) e insertar filas en tablas que hoy no tienen ninguna política RLS (T-10 no aplicada aún). Se añaden `SUPABASE_SERVICE_ROLE_KEY_DEV`/`_PROD` a `.env.ejemplo`, con el mismo régimen que `SUPABASE_ACCESS_TOKEN`: solo en `.env.local` del dueño, el agente no la usa nunca. Idempotencia por comprobación (si `CENTROS_SEMILLA[0]` ya existe, no hace nada más), no por upsert | Usar la clave anónima e insertar como si fuera un `administrator` autenticado; posponer la semilla entera hasta que T-10 tenga políticas | La clave anónima no basta: sin políticas de T-10 todavía no hay forma de autenticar como `administrator` y que las políticas dejen escribir. `service_role` (que además crea usuarios reales, algo que ninguna clave de PostgREST puede hacer) es la única vía hoy, y es exactamente el mismo perfil de riesgo que ya se aceptó para `SUPABASE_ACCESS_TOKEN` — nunca en el entorno del agente, solo en la máquina del dueño. |
| 2026-08-27 | T-07 | El test de fuga de secretos (punto 23, `herramientas/verificarFugaSecretos.test.ts`) invoca `tsc -b tsconfig.build.json` **desde dentro del propio test** (vía `execFileSync`, sin red ni credenciales) en vez de asumir que `dist/` ya existe y está actualizado | Asumir que `npm run build` ya corrió antes (el propio `npm test` se ejecuta antes de `build` en la verificación pre-push de §0.1) y leer `dist/` tal cual está | Depender del orden de la verificación pre-push habría hecho que este test pasara "por accidente" con un `dist/` desactualizado de una sesión anterior, o fallara con un `dist/` inexistente en un checkout limpio. Construyendo su propio `dist/` dentro del test, el resultado es correcto sin importar en qué orden se invoquen los scripts de `package.json` — el coste es que `npm test` tarda algo más y, como efecto colateral, deja `dist/` construido antes de que corra `npm run build` (que entonces es casi instantáneo, `tsc -b` es incremental). |
| 2026-08-27 | T-07 | Los dos CLI del dueño (`npm run migrate`, `npm run seed`) cargan `.env.local` en `process.env` ellos mismos, a través de `herramientas/cargarEnvLocal.ts`, que envuelve el cargador nativo de Node (`process.loadEnvFile`). La ruta se resuelve desde `import.meta.url`, no desde el `cwd`; si el fichero no existe no es un error (se sigue con las variables del entorno) y ambos CLI imprimen qué fichero cargaron y cuántas variables trae, antes de resolver credenciales | Añadir `dotenv` (rompe `dependencies` vacío, §0.2); pasar `--env-file=.env.local` en los scripts de `package.json` (depende del `cwd` y aborta con ENOENT donde no hay fichero, p. ej. el CI); exigir al dueño que exporte las variables a mano antes de cada comando | **Bug real reportado por el dueño al intentar aplicar la migración 001:** nada metía `.env.local` en `process.env` — `migrar.ts` y `seed.ts` leen `process.env` directamente, `dependencies` está vacío (no hay `dotenv`) y los scripts npm no pasaban `--env-file`. Con un `.env.local` correcto, `npm run migrate` afirmaba que faltaba `SUPABASE_ACCESS_TOKEN`: el mensaje era exacto sobre el síntoma y falso sobre la causa. Ningún test lo detectó porque toda la suite inyecta el entorno (`resolverCredenciales(env, ...)`) y el único fichero sin test directo era precisamente el wiring. `process.loadEnvFile` no añade dependencia, normaliza los finales de línea CRLF de Windows sin colar el retorno de carro en el valor, y **no pisa** las variables ya presentes en el entorno, así que los secretos del CI siguen ganando al fichero |
| 2026-08-27 | T-07 | El test de fuga de secretos invoca el compilador como `execFileSync(process.execPath, ['node_modules/typescript/bin/tsc', '-b', 'tsconfig.build.json'])`, en vez de ejecutar `node_modules/.bin/tsc` | Resolver `tsc.cmd` cuando `process.platform === 'win32'`; lanzar el comando a través de un shell; dejarlo como estaba y documentar que ese test solo pasa en Linux | `node_modules/.bin/tsc` es un shim de shell sin extensión: en Windows `execFileSync` no puede lanzarlo (`ENOENT`; el ejecutable real es `tsc.cmd`), así que el test fallaba en la máquina del dueño mientras pasaba en el CI (Ubuntu) — un test verde en CI y rojo en local es peor que un test que falla en los dos sitios. Invocar el entry JS de TypeScript con el propio Node no depende del shell ni del sistema operativo |
| 2026-08-27 | T-08 | Sin bundler no hay `import.meta.env`: la URL del proyecto y la clave anónima llegan al cliente mediante un `config.js` **plano** (JavaScript, no TypeScript, no pasa por `tsc`) que `index.html` carga con una etiqueta `<script>` normal ANTES de `dist/ui/main.js`, y que asigna `window.__CONFIG__ = { SUPABASE_URL, SUPABASE_ANON_KEY }`. `config.js` está en `.gitignore` (mismo régimen que `.env.local`) y `config.ejemplo.js`, commiteado, es la plantilla sin valores (mismo patrón que `.env.ejemplo`). `src/datos/configuracion.ts` (`leerConfiguracionEntorno`) valida el objeto ya leído y NUNCA lee `window` directamente — recibe `window.__CONFIG__` como parámetro desde `src/ui/main.ts`, mismo patrón de inyección que `instalarCapturaErrores` (T-05) con `window` — así que se testea sin `jsdom`. Sin `config.js` desplegado (el hosting sigue `<pendiente>`, T-25) la aplicación arranca igual: `main.ts` captura `ErrorConfiguracionFaltante` y solo se pierde el envío remoto de errores no controlados, verificado en Chromium headless (mismo método que T-00) | Usar variables `data-*` en la etiqueta `<script type="module">` de `main.js` y leerlas de `document.currentScript`; o generar un `.ts` con los valores interpolados en el propio paso de `tsc -b` | `data-*` en el script módulo exigiría parsear atributos del DOM para dos cadenas simples, más frágil que una asignación directa a un global; y generar un `.ts` con valores interpolados mezclaría datos de despliegue con código fuente versionado, exactamente lo que se evita con `.env.local`. Un `config.js` plano es el equivalente exacto de `.env.local` para el navegador: mismo mecanismo mental, mismo `.gitignore`, misma plantilla `.ejemplo`, y no necesita que el hosting entienda nada de TypeScript. |
| 2026-08-27 | T-08 | Taxonomía de errores de dominio (`src/datos/erroresDominio.ts`): ocho clases (`NoAutenticado`, `SinPermiso`, `Conflicto`, `ErrorDeValidacion`, `ErrorDeRed`, `ErrorDelServidor`, `FicheroDemasiadoGrande`, `TipoDeFicheroNoPermitido`), cada una con un mensaje por defecto ya en español pero que **nadie debe mostrar directamente al usuario** — ver la siguiente decisión sobre `mensajesAbuso.ts`. `errorDeRespuesta(respuesta)` traduce por código de estado (`401→NoAutenticado`, `403→SinPermiso`, `409→Conflicto`, `400`/`422→ErrorDeValidacion`, `413→FicheroDemasiadoGrande`, `415→TipoDeFicheroNoPermitido`, `5xx→ErrorDelServidor`, cualquier otro código también `ErrorDelServidor`), usando el `message` de Postgres/PostgREST cuando lo hay para `Conflicto`/`ErrorDeValidacion` (información técnica útil para logs, nunca para la interfaz) | Un único `ErrorSupabase` con una propiedad `tipo: 'no_autenticado' \| 'sin_permiso' \| ...` en vez de ocho clases | El requisito 4 de T-08 pide explícitamente "errores de dominio tipados" con esos ocho nombres; clases separadas permiten `instanceof` en vez de comparar una cadena, y es el mismo patrón ya establecido por `ErrorLimiteAlcanzado` (T-06) — un tipo de error, una clase, nunca una propiedad de discriminación genérica. |
| 2026-08-27 | T-08 | `mensajeAmigable` (`src/nucleo/mensajesAbuso.ts`, T-06) se amplía, tal como su propio comentario de T-06 preveía, con un mensaje fijo en español por cada una de las ocho clases de `erroresDominio.ts` — **nunca** `error.message`, aunque el constructor de cada clase ya tenga un mensaje razonable en español. Un test explícito (`Conflicto NUNCA expone el message crudo de Postgres`) construye un `Conflicto`/`ErrorDeValidacion` con un mensaje técnico real de Postgres (`duplicate key value violates unique constraint...`) y comprueba que no aparece en el resultado | Usar `error.message` directamente para `Conflicto`/`ErrorDeValidacion`, ya que sus constructores admiten un mensaje personalizado que a veces sí es legible | `errorDeRespuesta` (T-08) pasa el `message` de Postgres a `Conflicto`/`ErrorDeValidacion` cuando existe, y ese texto puede ser perfectamente técnico o estar en inglés (`"duplicate key value violates unique constraint \"alumno_pkey\""`). El requisito de T-06 es explícito: "nunca revela el error original". El `.message` de estas clases queda como información de depuración (para el logger, T-02, que ya lo depura de datos sensibles pero no lo traduce) — la traducción a usuario vive solo en `mensajesAbuso.ts`, con texto fijo. |
| 2026-08-27 | T-08 | Codificador de valores de filtro (`src/datos/codificadorValores.ts`, requisito 5): dos capas siempre aplicadas en el mismo orden — (1) escapado sintáctico de PostgREST (envolver entre comillas dobles y escapar `"`/`\` internos cuando el valor contiene alguno de `, . ( ) " \` o tiene espacios en los extremos o está vacío) y (2) `encodeURIComponent` del resultado. Las columnas y la cadena de `select` (incluidos recursos embebidos como `centro:centro_estudios(id,nombre)`) **no** pasan por aquí: los escribe quien programa, no son datos de usuario — mismo criterio que ya aplica el `mensaje` fijo del logger (T-02) frente a su `contexto` variable | Usar `encodeURIComponent` sin más, confiando en que PostgREST no necesita nada adicional; o construir la query con `URLSearchParams` | `encodeURIComponent` solo no evita que un valor de usuario con una coma real rompa un `in.(a,b)` (la coma del dato se confundiría con el separador de la lista): hace falta la capa PostgREST antes de percent-encodear. `URLSearchParams` se descartó para la query completa porque percent-encodea TODO por igual (application/x-www-form-urlencoded), incluidas las comas y paréntesis estructurales de `in.(...)` que deben quedar literales — habría roto la sintaxis de PostgREST. Un test dedicado (`codificarValorFiltro`/`codificarListaFiltro`) cubre comillas, comas (incluida la lista con un valor que contiene una coma real, verificando que sigue partiéndose en el número correcto de elementos), `%` literal y los caracteres reservados. |
| 2026-08-27 | T-08 | Cliente de PostgREST (`src/datos/postgrest.ts`) con API fluida `cliente.desde<T>('tabla').eq(...).eq(...).seleccionar('columnas')` (o `.insertar`/`.actualizar`/`.eliminar`), un único builder que acumula filtros/orden/límite/rango y decide el verbo HTTP en el método terminal, en vez de builders separados por operación. `select`/`order`/`limit` van en la query string; el rango de paginación va en las cabeceras `Range`/`Range-Unit` (no en query params `limit`/`offset`), y `seleccionarConTotal` añade `Prefer: count=exact` y lee el total de `Content-Range` (`null` si PostgREST devuelve `.../*`). `eq(columna, null)` genera `is.null`, no `eq.null` (la sintaxis correcta de PostgREST para nulos) | Un builder distinto por verbo (`ConstructorSelect`, `ConstructorInsert`, ...); usar `limit`/`offset` como query params para la paginación en vez de la cabecera `Range` | Un único builder es más simple de usar (`.eq(...)` sirve tanto para filtrar un `select` como para acotar un `update`/`delete`, que es exactamente cómo se usan en la práctica: "el alumno con este id, actualízale este campo") y evita duplicar la lógica de filtros tres veces. La cabecera `Range` es el mecanismo de paginación "nativo" de PostgREST (documentado explícitamente para ese fin, a diferencia de `limit`/`offset` que son solo azúcar sobre la misma cabecera) y deja la query string libre para lo que sí es específico de cada petición. |
| 2026-08-27 | T-08 | Subconjunto de PostgREST implementado, y solo ese: `select` (con recursos embebidos), `eq`/`in`/`gte`/`lte`/`ilike`, `order`, `limit`, rango con total opcional, `insert`, `update`, `delete`, `rpc`. **No** implementado: `neq`, `not`, `or`/`and` compuestos, filtros sobre columnas de un recurso embebido, `upsert` explícito (`on_conflict`) | Implementar el superconjunto completo de operadores de PostgREST desde ahora, para no tener que ampliar el cliente más adelante | Ninguna tarea del roadmap hasta T-23 necesita más que este subconjunto (las pantallas son listados y fichas por tabla, con filtros simples). Construir operadores sin un caso de uso real que los ejercite (y por tanto los testee de verdad) es exactamente el tipo de trabajo especulativo que el protocolo de esta sesión pide evitar; la señal para ampliar `postgrest.ts` es que una tarea futura los necesite de verdad, documentado en la cabecera del propio fichero para que quien la escriba sepa dónde tocar. |
| 2026-08-27 | T-08 | Cliente de Storage (`src/datos/almacenamiento.ts`) con cuatro operaciones (`subir`, `eliminar`, `urlFirmada`, `urlFirmadasEnLote`) sobre cuatro endpoints asumidos de la API de Storage de Supabase (`POST /storage/v1/object/{bucket}/{ruta}`, `DELETE /storage/v1/object/{bucket}` con `{prefixes}`, `POST /storage/v1/object/sign/{bucket}/{ruta}` con `{expiresIn}`→`{signedURL}`, `POST /storage/v1/object/sign/{bucket}` con `{expiresIn, paths}`→array de `{path, signedURL, error}`), **sin poder verificarse contra documentación en vivo en esta sesión** — mismo aviso, y mismo motivo (sin salida de red a `supabase.com`), que T-06/T-07 dejaron para GoTrue y la Management API | Bloquear la parte de Storage de T-08 hasta poder verificar los endpoints; inventar un endpoint distinto sin decirlo | Igual que con el endpoint de la Management API (T-07): el resto del cliente (autenticación, traducción de errores, manejo de `Content-Range`, forma del resultado) no depende de que la ruta exacta sea correcta, y corregirla si hace falta es un cambio local a una función, no una reescritura. Documentado también en la cabecera del propio fichero para que si `npm run migrate`/la pantalla de avatares (T-14) da un `404` o una forma de respuesta inesperada, este sea el primer sospechoso. |
| 2026-08-27 | T-08 | `urlFirmadasEnLote(bucket, rutas, segundosValidez)` manda **una única petición HTTP** con todas las rutas en el cuerpo (`{expiresIn, paths}`), nunca N peticiones individuales — verificado con un test que cuenta las llamadas al doble de `fetch` y comprueba que valen 1 para 3 rutas | Llamar a `urlFirmada` en bucle, una petición por ruta | El requisito 3 de T-08 lo dice explícitamente: "no es un extra, es lo que hace viable la pantalla de pasar lista con la conexión de un aula" (T-19), y §0.2 lo repite como regla permanente ("las URL firmadas de una pantalla se piden SIEMPRE en lote, en una sola petición"). Un bucle sería la implementación obvia pero exactamente la que este requisito prohíbe; el test que cuenta peticiones existe para que una regresión futura (alguien "simplificando" `urlFirmadasEnLote` a un `Promise.all` de `urlFirmada`) la detecte inmediatamente. |
| 2026-08-27 | T-08 | Petición HTTP autenticada extraída a un módulo compartido (`src/datos/peticionHttp.ts`, `peticionAutenticada`) usado tanto por `postgrest.ts` como por `almacenamiento.ts`: cabeceras `apikey`/`authorization` (token de sesión si lo hay, si no la clave anónima), traducción de un fallo de red a `ErrorDeRed` (sin tocar un `AbortError`, que se propaga tal cual, T-06) y traducción de toda respuesta no exitosa a un error de dominio tipado | Duplicar esta lógica (idéntica) en los dos clientes; o meter también aquí el manejo específico de cada uno (`Prefer`/`Range` de PostgREST, `x-upsert`/firma de Storage) | Es la misma lógica exacta en los dos sitios (no una coincidencia superficial: los dos hablan HTTP con la misma Supabase, la misma clave anónima y el mismo régimen de errores), así que extraerla es eliminar duplicación real, no una abstracción especulativa. Lo que sí es distinto de cada cliente (qué cabeceras extra, qué forma de cuerpo) se queda en cada módulo, que arma sus propias opciones antes de llamar a la función compartida. |
| 2026-08-27 | T-08 | `src/datos/eventoError.ts` (T-05) se reescribe para usar `crearClientePostgrest(config).rpc('registrar_evento_error', ...)` en vez de su propia llamada a `fetch` — la "puerta única" que exige el objetivo de T-08 no tendría sentido si el primer consumidor real siguiera abriendo la suya. Su API pública (`crearEnviadorEventoError(config): EnviadorEventoError`) no cambia, así que `src/ui/main.ts` no necesita tocarse por este motivo; su suite de tests tampoco cambió una sola aserción | Dejar `eventoError.ts` como estaba (construía su propia petición `fetch`, escrito antes de que `postgrest.ts` existiera) y considerarlo deuda técnica para otra sesión | La duplicación era real y de comportamiento idéntico (mismas cabeceras, misma forma de cuerpo) — no había ninguna razón funcional para mantener dos caminos hacia la misma RPC. Refactorizarlo ahora, con la suite de tests existente como red, es más barato que dejarlo para una sesión futura que tenga que redescubrir la duplicación. |
| 2026-08-27 | T-08 | `src/ui/main.ts` ya conecta el envío remoto de `evento_error` de verdad: lee `window.__CONFIG__` con `leerConfiguracionEntorno`, y si hay configuración crea el enviador real (`crearEnviadorEventoError`) y se lo pasa a `crearInformadorErrores`; si `ErrorConfiguracionFaltante` (sin `config.js` desplegado), lo captura, registra un aviso en el logger local y sigue sin enviador remoto — la aplicación nunca deja de arrancar por esto. Sin `obtenerTokenSesion` todavía (T-09 no existe): usa la clave anónima, que es válida para esta RPC porque `registrar_evento_error` (contrato de T-05) debe poder registrar errores incluso sin sesión | Esperar a T-09 (autenticación) para conectar el envío remoto, ya que hasta entonces no hay token de sesión que pasar | El valor de persistir errores no controlados en el servidor es independiente de si hay sesión o no — un error en la pantalla de login, antes de autenticarse, es exactamente el caso que más interesa capturar. T-09 solo tiene que añadir `obtenerTokenSesion` a esta misma llamada cuando exista un lector de sesión, no construir la integración desde cero. |
| 2026-08-27 | T-08 | `exactOptionalPropertyTypes: true` (activo desde T-00) prohíbe asignar `undefined` explícitamente a una propiedad opcional (`p?: T`) salvo que su tipo declare `T \| undefined` de forma expresa: reconstruir un objeto copiando un campo opcional de otro (`{ ...destino, obtenerTokenSesion: origen.obtenerTokenSesion }`, o pasar `body: cuerpoQuePodriaSerUndefined` a `RequestInit`) falla en `tsc --noEmit` con `TS2379`, un error que no aparece hasta que se escribe ese patrón concreto (T-07 documentó un problema distinto pero de la misma familia: algo que ni el tipo de la firma ni una lectura superficial delatan). Solución aplicada donde ha hecho falta: spread condicional (`...(x !== undefined ? { clave: x } : {})`) para omitir la clave por completo en vez de asignarle `undefined`, o declarar el campo interno como `T \| undefined` explícito cuando el propio módulo controla los dos lados | Desactivar `exactOptionalPropertyTypes` para evitar el problema | La flag está en `tsconfig.json` desde T-00 y captura una clase real de bugs (confundir "campo ausente" con "campo presente mientras vale `undefined`", que PostgREST y otras API REST sí distinguen: un `body: undefined` en `fetch` no es lo mismo que omitir `body`). Se documenta aquí, igual que T-07 documentó las propiedades de parámetro, para que ninguna sesión futura pierda tiempo redescubriendo el mismo patrón al escribir el primer módulo que reconstruye un objeto con campos opcionales. |
| 2026-08-27 | T-09 | El bloqueo tras tres contraseñas falladas se implementa **en la base de datos y se hace efectivo por RLS**: marca de bloqueo en `perfil`, conteo de intentos mantenido por una RPC `SECURITY DEFINER` llamable por `anon` (quien falla el login todavía no tiene sesión), y la condición "no bloqueado" en **todas** las políticas de T-10, no solo en `perfil`. El inicio de sesión (requisito 7 de T-09) rechaza al usuario bloqueado igual que ya rechaza a `activo = false`. El desbloqueo lo hace el administrador desde la aplicación; el bloqueo alcanza también al administrador, cuya vía de escape es el editor SQL del panel (solo el dueño), documentada en `DEVELOPERS.md` | Contar los fallos solo en el cliente (lo más barato, pero **eludible con `curl`**: el login va del navegador directo a GoTrue, así que no sería un control de acceso sino disuasión); proxy del login en una Edge Function (control real y sin exponer la RPC a `anon`, pero es un **backend propio**, vetado por §0.2); bloqueo con caducidad automática; no implementarlo y quedarse con el límite por IP de GoTrue | Decisión del **dueño** (respuesta a #4 y #5 de §6, 2026-08-27), no autónoma. La base de datos es el único punto donde el bloqueo se aplica de verdad sin añadir backend: un atacante que acierte la contraseña obtiene un token válido pero no puede leer nada. Contrapartida **aceptada explícitamente por el dueño**: como el conteo lo reporta un cliente sin autenticar, cualquiera que conozca el email de un profesor puede dejarlo fuera (denegación de servicio por cuenta); no sirve para *entrar*, solo para *cerrar*. La RPC de conteo debe responder lo mismo exista o no el email (requisito 9 de T-09: no revelar si una cuenta existe) |
| 2026-08-27 | T-09 | «El administrador renueva la contraseña» se implementa **disparando el correo de recuperación** (`POST /auth/v1/recover`, que funciona con la clave anónima), no fijando el administrador una contraseña nueva. Es exactamente el flujo que ya exigía el requisito 2 de T-09, ahora también accesible desde la ficha de usuario del administrador | Que el administrador fije una contraseña temporal con la API admin de GoTrue (`PUT /auth/v1/admin/users/{id}`) | Esa API exige `service_role`, prohibida en el navegador (§0.2 y §0.1), así que obligaría a una Edge Function, es decir, a un backend propio que este proyecto no tiene. La vía elegida no cambia el stack y mantiene la propiedad que ya buscaba la spec: **el administrador no conoce la contraseña de nadie, nunca**. Depende del SMTP: si el del plan gratuito no llega, el requisito 3 de T-09 ya obliga a abrir fila en §3 para que el dueño configure uno propio |
| 2026-08-27 | T-09 | Cliente propio de GoTrue (`src/datos/autenticacion.ts`): `iniciarSesion`/`cerrarSesion`/`renovarSesion`/`solicitarRecuperacionContrasena`/`establecerContrasenaNueva` sobre `/auth/v1/token`, `/auth/v1/logout`, `/auth/v1/recover`, `/auth/v1/user`, con una función `peticionGoTrue` propia (no reutiliza `peticionAutenticada` de T-08, que fija el `Authorization` a "token de sesión actual o clave anónima" — GoTrue necesita un `Bearer` distinto en cada llamada: la clave anónima para login/logout/recover, y el token de recuperación, no el de sesión, para `establecerContrasenaNueva`) | Ampliar `peticionAutenticada`/`OpcionesAutenticacion` (T-08) para aceptar un `Bearer` ad-hoc por llamada | `peticionAutenticada` está pensada para un cliente con una única sesión "actual" inyectada por cierre (`obtenerTokenSesion: () => token`), que es exactamente el patrón que necesitan `postgrest.ts`/`almacenamiento.ts` y el propio `gestorSesion.ts` al construir su cliente de perfil. Forzarla a aceptar un Bearer distinto en cada llamada individual habría acoplado una necesidad de un solo módulo (GoTrue, que gestiona sus propios tokens en tránsito antes de que exista una "sesión") a una abstracción compartida por dos módulos que no la necesitan. La duplicación resultante (unas 20 líneas: cabeceras, `fetch`, traducción de fallo de red) es pequeña y ambas versiones reutilizan igual `errorDeRespuesta`/`esFalloDeRed`/`ErrorDeRed` de `erroresDominio.ts`, así que no hay dos taxonomías de error. |
| 2026-08-27 | T-09 | GoTrue responde `400` (no `401`) a `grant_type=password` con credenciales incorrectas: `iniciarSesion` distingue ese caso explícitamente y lanza `CredencialesInvalidas` (clase nueva, en `autenticacion.ts`, fuera de las ocho de T-08) en vez de dejar que `errorDeRespuesta` lo traduzca a `ErrorDeValidacion` (que sería engañoso: no es un problema de formato de los datos enviados, es que no coinciden con ninguna cuenta). Un `refresh_token` inválido en `renovarSesion` (también `400`) se traduce en cambio a `NoAutenticado` (T-08), reutilizada tal cual: significa exactamente lo mismo, "no hay sesión utilizable" | Reutilizar `NoAutenticado` también para credenciales de login incorrectas; o traducir el `400` de login con `errorDeRespuesta` sin caso especial | `NoAutenticado` (T-08) tiene el mensaje por defecto "no has iniciado sesión, o tu sesión ha caducado" — describe perfectamente un `refresh_token` caducado, pero no un intento de login con la contraseña equivocada (que sí es un primer intento, no una sesión que caduca). Distinguir las dos situaciones con clases distintas es lo que le permite a `mensajesAbuso.ts` dar un mensaje apropiado a cada una sin inspeccionar texto. |
| 2026-08-27 | T-09 | Persistencia de sesión (`src/nucleo/almacenSesion.ts`, requisito 4 — "la opción más conservadora posible y documentada, riesgo de XSS explícito"): se persiste **solo el `refresh_token`**, en **`sessionStorage`** (no `localStorage`), bajo una única clave propia; el `access_token` vive exclusivamente en memoria dentro de `gestorSesion.ts` y se vuelve a pedir uno nuevo en cada arranque (`renovarSesion` con el `refresh_token` persistido) | (a) `localStorage` (persiste indefinidamente, es lo que hace por defecto el SDK de Supabase que este proyecto no usa); (b) memoria pura, sin persistencia alguna; (c) persistir también el `access_token` | Ninguna opción de almacenamiento en el navegador es inmune a un XSS que ejecute JS con el mismo origen — eso es lo que hace "conservador" a esto una elección de grado, no una garantía. `sessionStorage` sobrevive a una recarga accidental de la pestaña (el caso real que motiva el requisito 5, "que no le caduque la sesión con la lista a medias") pero se borra sola al cerrar la pestaña, a diferencia de `localStorage`, que persistiría indefinidamente en un dispositivo compartido. No persistir el `access_token` acota lo que un XSS que sí llegara a leer el almacenamiento obtendría: un token que por sí solo solo sirve para pedir uno nuevo (`/auth/v1/token?grant_type=refresh_token`), no para autenticar peticiones directamente. La mitigación de fondo sigue siendo la prohibición de `innerHTML` (T-01) y, más adelante, la CSP de T-25 — este almacén no sustituye a esas dos, las complementa. |
| 2026-08-27 | T-09 | Renovación de sesión estrictamente **proactiva**, nunca reactiva a un `401`: `gestorSesion.renovarAlAbrirPasarLista()` es el único punto que renueva, comparando `expiraEnMs` contra el reloj inyectado con un margen (`MARGEN_RENOVACION_POR_DEFECTO_MS = 5 minutos`, mismo criterio de "cifra de partida documentada, ajustable" que los contratos recomendados de límites de T-06); no existe ningún interceptor que reintente una petición tras un `401`. Si la renovación proactiva falla, el error se **relanza** sin tocar `sesionActual` ni el `EstadoSesion`: la sesión sigue "autenticada" con el token viejo hasta que caduque de verdad | Interceptar `401` en `peticionAutenticada`/`postgrest.ts` (T-08) y reintentar automáticamente tras renovar | El requisito 5 de T-09 es explícito y con motivo de producto detrás ("nunca esperar al 401": a un profesor no se le puede caducar la sesión con la lista a medias). Un interceptor reactivo sería más "automático" pero contradice la letra de la spec y complicaría `postgrest.ts`/`almacenamiento.ts` (T-08, ya cerrados y testeados) con reintentos y relación de dependencia circular hacia `gestorSesion.ts`. El punto de enganche proactivo se deja explícitamente nombrado (`renovarAlAbrirPasarLista`) para que T-19 solo tenga que llamarlo al montar la pantalla de pasar lista, sin diseño nuevo. |
| 2026-08-27 | T-09 | `gestorSesion.ts` construye su **propio** `ClientePostgrest` (T-08) internamente para cargar el `perfil` propio, con `obtenerTokenSesion: () => accessToken` cerrado sobre el token recién obtenido de GoTrue — no recibe un `ClientePostgrest` ya construido desde fuera | Aceptar un `ClientePostgrest` inyectado por quien llama a `crearGestorSesion` | Un `ClientePostgrest` normal necesitaría su `obtenerTokenSesion` apuntando AL PROPIO `gestorSesion` (para que las peticiones posteriores usen la sesión vigente) — pero en el instante de `iniciarSesion`/`restaurar`, el gestor todavía no ha decidido si esta sesión es válida (perfil activo, rol legítimo): sería una referencia circular (el cliente necesita al gestor, que todavía está decidiendo si va a existir como "sesión válida"). Construir un cliente de usar-y-tirar por cada carga de perfil, cerrado sobre el token que ya se tiene en la mano, rompe el ciclo sin necesitar ningún patrón de inicialización diferida. |
| 2026-08-27 | T-09 | Ningún módulo de `src/ui/` toca el `document` global: `formularios.ts` y las cinco pantallas reciben un `Document` explícito (normalmente `contenedor.ownerDocument`, resuelto por quien llama) para crear elementos, en vez de llamar a `document.createElement` directamente | Llamar a `document.createElement` directamente, como hace implícitamente cualquier página web normal | El proyecto no tiene ningún polyfill de `document` en el entorno de test (`node --test` corre en Node puro; `jsdom` solo se instancia explícitamente dentro de cada test, nunca como global) — un `document.createElement` directo habría hecho que estas pantallas fueran imposibles de testear sin además parchear `globalThis.document`, rompiendo el principio de inyección que ya siguen `Reloj`/`Temporizador`/`instalarCapturaErrores(window, ...)`/`Storage` (T-03/T-06/T-09). `contenedor.ownerDocument` da el documento correcto tanto en un navegador real como en un contenedor montado con `jsdom`, sin necesitar un parámetro adicional en la firma pública de cada pantalla (`mostrarPantallaX(contenedor, deps)` se mantiene con la misma forma que ya tenía `mostrarPantallaInicial`, T-00). |
| 2026-08-27 | T-09 | Objetivos táctiles (§0.2, "≥44px") y tamaño de fuente de los campos (16px, evita el zoom automático de iOS) se fijan como estilos en línea (`elemento.style.minHeight = '44px'`, etc.) dentro de `formularios.ts`, no en una hoja de estilos | Crear ya una hoja CSS (`estilos.css` o similar) | El proyecto no tiene ningún sistema de estilos todavía — no hay una sola regla CSS en el repositorio antes de esta sesión, y ninguna tarea hasta ahora (T-00 a T-08) lo necesitó. Introducir una hoja de estilos completa es una decisión de diseño visual que excede el alcance de T-09 (autenticación, no maquetación) y que una tarea de UI futura (T-16/T-19, cuando haya pantallas con más de un formulario simple) puede resolver de una vez para toda la aplicación, en vez de que cada pantalla de T-09 improvise la suya. Los estilos en línea son el mínimo que cumple el requisito de accesibilidad sin prejuzgar esa decisión. |
| 2026-08-28 | P-01 | La migración `db/002_bloqueo_cuenta.sql` **redefine `rol_actual()`** (`create or replace function`, conservando `security definer`) añadiendo `and not bloqueado` a su única consulta, en vez de tocar cada política de T-10 una por una. Como `es_administrator()`/`es_teacher()` llaman a `rol_actual()`, y T-10 (requisito 1 de su spec) ya debe escribir **todas** sus políticas nuevas usando esas dos funciones en vez de repetir la lógica de rol, el efecto "no bloqueado" se propaga a las siete tablas nuevas y al bucket de avatares sin una sola línea extra en T-10 | Añadir `and not bloqueado` como condición explícita en cada política de T-10 (siete tablas + bucket, cada una repitiendo la comprobación); crear una función nueva `no_bloqueado()` para componerla aparte de `es_administrator()`/`es_teacher()` | La alternativa de repetirlo en cada política es exactamente el tipo de duplicación que el propio requisito 1 de T-10 prohíbe ("una sola definición, nunca repetir la lógica de rol dentro de una política") y además es frágil: basta con que una sesión futura escriba una política nueva sin acordarse de la condición para que el bloqueo quede en cosmética — el riesgo que la propia SEGUIMIENTO.md señalaba como "lo urgente" de esta migración. Bakearlo en `rol_actual()` lo hace estructuralmente imposible de olvidar. La función `no_bloqueado()` aparte se descartó por ser una abstracción de un solo uso: no hay ningún caso previsto en que se necesite comprobar "no bloqueado" sin comprobar también un rol concreto. `perfil_leer_propio` (bootstrap) no usa `rol_actual()`, así que un perfil bloqueado sigue leyendo su propia fila — imprescindible para que la aplicación pueda explicarle que está bloqueado |
| 2026-08-28 | P-01 | `perfil.intentos_fallidos` **no se resetea al iniciar sesión con éxito**, solo cuando `admin_desbloquear_usuario()` levanta un bloqueo. El bloqueo dispara al llegar a 3 fallos **acumulados**, no necesariamente consecutivos | (a) resetear el contador a `0` en cada login correcto, para que el bloqueo sea "3 fallos consecutivos" de verdad; (b) combinar la carga del perfil propio y el reseteo en una única RPC nueva (`cargar_mi_perfil()`, sustituyendo el `select` directo de `gestorSesion.cargarPerfilOFallar`) para mantener el reseteo sin gastar una segunda petición | La opción (a) tal cual (una llamada RPC de reseteo aparte, tras cargar el perfil) rompe un requisito ya cerrado y con test propio desde T-09: "un `student`/rol desconocido dispara como máximo UNA llamada de datos al autenticar" (`gestorSesion.test.ts`, "UNA sola llamada de datos, nunca más") — el reseteo tendría que dispararse para todos los roles por igual, no solo para quien va a usar la sesión, así que rompería ese test para todos. La opción (b) lo evita sin tocar el conteo, pero cambia el mecanismo de carga de perfil que T-08/T-09 ya cerraron y testearon (de un `select` con filtro a una RPC con lectura+escritura combinadas) por un beneficio menor: la semántica "3 consecutivos" no la pide expresamente el dueño (§6 #5 solo dice "al tercer intento fallido"), y un contador que solo baja por acción del administrador es una interpretación razonable y más simple de la misma decisión. Si en el futuro se quiere semántica estrictamente consecutiva, la opción (b) queda documentada aquí como la vía que no rompe el requisito de una sola llamada |
| 2026-08-28 | P-01 | `registrar_intento_fallido(p_email)` solo incrementa `intentos_fallidos` `where ... and not bloqueado`: una vez bloqueada la cuenta, más llamadas no siguen sumando | Dejar que el contador crezca sin límite mientras la cuenta sigue bloqueada | Sin tope, cualquiera que siga llamando a la RPC (`anon`, sin más límite que el que GoTrue aplique por IP al propio intento de login que la dispara) podría hacer crecer el entero indefinidamente contra una cuenta ya bloqueada — no cambia el resultado (sigue bloqueada) pero no aporta nada y es una superficie de recurso gratuita para desperdiciar. La condición es una línea en el `WHERE` del `UPDATE`, no una guarda adicional |
| 2026-08-28 | P-01 | `GestorSesion.desbloquearUsuario(usuarioId)` se añade ya en `gestorSesion.ts`, con sus tests contra dobles, aunque **no tiene consumidor todavía**: la pantalla de administración de usuarios es T-24, que sigue `PENDIENTE` y depende de T-10 | Esperar a T-24 para escribir esta función, ya que hasta entonces ninguna pantalla la llama | Mismo criterio que T-08 aplicó a `eventoError.ts` conectándose antes de que existiera sesión (fila de T-08, 2026-08-27): el valor de tener la pieza de datos lista y testeada es independiente de si ya hay una pantalla que la use, y evita que T-24 tenga que diseñar y testear el acceso a la RPC desde cero — solo construye la interfaz sobre una función ya cerrada. La RPC en sí (`admin_desbloquear_usuario`) exige `es_administrator()` en el servidor, así que no hay ningún riesgo de exponer una acción sin control por no tener aún interfaz que la invoque |
